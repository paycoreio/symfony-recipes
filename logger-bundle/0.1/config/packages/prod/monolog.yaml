monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: stdout
            excluded_http_codes: [404, 405]
            buffer_size: 50
